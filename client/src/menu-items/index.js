import journal from './journal';
import qnotes from './qnotes';
import todo from './todo';
import budget from './budget'
import calendar from './calendar'


// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [todo, journal, qnotes,budget, calendar  ]
};

export default menuItems;
